# openapi-typescript@7.x declares peer typescript@"^5.x" but works correctly
# with TypeScript 6 (verified: npm run generate succeeds). The peer-dep range
# is just stale. Remove this line once openapi-typescript publishes a release
# that declares support for TypeScript 6.
legacy-peer-deps=true
