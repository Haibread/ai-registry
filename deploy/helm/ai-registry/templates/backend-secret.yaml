{{- /*
This secret is rendered only when the user provides a plain-text databaseUrl
directly in values (i.e., not using an external pre-existing secret).
When databaseUrlSecret is set, users are expected to create that secret
themselves; this template produces nothing.
*/}}
{{- if and .Values.backend.databaseUrl (not .Values.backend.databaseUrlSecret) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-registry.backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-registry.backend.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
stringData:
  DATABASE_URL: {{ .Values.backend.databaseUrl | quote }}
{{- end }}
